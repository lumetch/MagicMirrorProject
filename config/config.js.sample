/* Config Sample
 *
 * For more information on how you can configure this file
 * see https://docs.magicmirror.builders/configuration/introduction.html
 * and https://docs.magicmirror.builders/modules/configuration.html
 */
let config = {
	address: "localhost",
	port: 8080,
	basePath: "/",
	ipWhitelist: ["127.0.0.1", "::ffff:127.0.0.1", "::1"],
	useHttps: false,
	httpsPrivateKey: "",
	httpsCertificate: "",
	language: "en",
	locale: "en-US",
	logLevel: ["INFO", "LOG", "WARN", "ERROR"],
	timeFormat: 24,
	units: "metric",
	timezone: "Europe/Zurich",

	modules: [

		// ─── Always visible (fixed across all pages) ─────────────────────────────

		{
			module: "alert"
		},
		{
			module: "updatenotification",
			position: "top_bar"
		},
		{
			module: "MMM-Remote-Control",
			config: {
				apiKey: ""
			}
		},
		{
			// Fullscreen Jarvis overlay — activates on wake word, covers all pages.
			// Jabra SPEAK 510 USB: card 2 → plughw:2,0
			module: "MMM-LocalAssistant",
			position: "fullscreen_above",
			config: {
				whisperPath: "/home/lumetch/whisper.cpp/build/bin/whisper-cli",
				whisperModel: "/home/lumetch/whisper.cpp/models/ggml-tiny.en.bin",
				wakeWordModel: "hey_jarvis_v0.1",
				wakeWordThreshold: 0.5,
				captureSeconds: 5,
				espeakVoice: "en-gb",
				micDevice: "plughw:2,0"
			}
		},

		// ─── Page 1 — Information ─────────────────────────────────────────────────

		{
			module: "clock",
			position: "top_left",
			config: {
				timezone: "Europe/Zurich"
			}
		},
		{
			module: "weather",
			position: "top_right",
			header: "Now",
			config: {
				weatherProvider: "openmeteo",
				type: "current",
				lat: 47.3769,
				lon: 8.5417,
				units: "metric"
			}
		},
		{
			module: "calendar",
			header: "Calendar",
			position: "upper_third",
			config: {
				calendars: [
					{
						fetchInterval: 7 * 24 * 60 * 60 * 1000,
						symbol: "calendar-check",
						// Google Calendar → Settings → "Integrate calendar" → "Secret address in iCal format"
						url: "https://calendar.google.com/calendar/ical/YOUR_CALENDAR_ID/basic.ics"
					}
				],
				maximumEntries: 3
			}
		},
		{
			// Continuous ambient Jarvis animation — slow-breathing concentric rings in light blue.
			module: "MMM-JarvisAmbient",
			position: "middle_center"
		},
		{
			module: "newsfeed",
			position: "lower_third",
			config: {
				feeds: [
					{
						title: "Krebs on Security",
						url: "https://krebsonsecurity.com/feed/"
					},
					{
						title: "The Hacker News",
						url: "https://feeds.feedburner.com/TheHackersNews"
					},
					{
						title: "Dark Reading",
						url: "https://www.darkreading.com/rss.xml"
					},
					{
						title: "SANS ISC",
						url: "https://isc.sans.edu/rssfeed_full.xml"
					}
				],
				showSourceTitle: true,
				showPublishDate: true,
				broadcastNewsFeeds: true,
				broadcastNewsUpdates: true
			}
		},
		{
			module: "weather",
			position: "bottom_left",
			header: "5-Day Forecast",
			config: {
				weatherProvider: "openmeteo",
				type: "forecast",
				lat: 47.3769,
				lon: 8.5417,
				units: "metric",
				maxNumberOfDays: 5
			}
		},

		// ─── Page 2 — Smart Home ──────────────────────────────────────────────────

		{
			module: "MMM-PhilipsHue",
			position: "upper_third",
			header: "Lights",
			config: {
				// Run: node modules/MMM-PhilipsHue/scripts/hue-pair.js <bridge-ip>
				// to get the username after pressing the link button on the bridge.
				bridgeIp: "YOUR_HUE_BRIDGE_IP",
				username: "YOUR_HUE_USERNAME",
				pollInterval: 30000,
				rooms: ["Office", "Living Room", "Bedroom"]
			}
		},
		{
			// Spotify now-playing display.
			// OAuth tokens needed — see MMM-OnSpotify README for auth setup.
			module: "MMM-OnSpotify",
			position: "middle_center",
			config: {
				clientID: "YOUR_SPOTIFY_CLIENT_ID",
				clientSecret: "YOUR_SPOTIFY_CLIENT_SECRET",
				accessToken: "",
				refreshToken: "",
				broadcastSpotifyStatus: true,
				displayWhenEmpty: "clock",
				userAffinityUsage: "TOP_TRACKS",
				showDebugPalette: false
			}
		},

		// ─── Page management ──────────────────────────────────────────────────────

		{
			// MMM-pages uses explicit module name lists — NOT classes on each module.
			// Modules not listed here AND not in fixed[] are hidden on all pages.
			module: "MMM-pages",
			config: {
				modules: [
					["clock", "weather", "calendar", "MMM-JarvisAmbient", "newsfeed"],
					["MMM-PhilipsHue", "MMM-OnSpotify"]
				],
				fixed: [
					"alert",
					"updatenotification",
					"MMM-Remote-Control",
					"MMM-LocalAssistant"
				],
				rotationTime: 60000,
				animationTime: 1000
			}
		}
	]
};

/*************** DO NOT EDIT THE LINE BELOW ***************/
if (typeof module !== "undefined") { module.exports = config; }
